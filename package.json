{
  "name": "xc-mongo-db",
  "version": "0.0.1",
  "author": "Fran Cadaval",
  "description": "This library provides support for the MongoDB database.",
  "license": "ISC",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npm run clean & tsc",
    "clean": "tsc --build --clean & rimraf dist",
    "test": "jest",
    "e2e-test": "npm run build & node ./dist/e2e-test.js"
  },
  "keywords": [
    "mongodb"
  ],
  "dependencies": {
    "@nestjs/common": "^10.3.8",
    "@nestjs/core": "^10.3.8",
    "mongodb": "^6.6.2"
  },
  "devDependencies": {
    "@nestjs/testing": "^10.3.9",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.12.12",
    "jest": "^29.7.0",
    "rimraf": "^6.0.1",
    "ts-jest": "^29.1.4",
    "ts-jest-mocker": "^1.0.0",
    "typescript-eslint": "^7.13.0"
  }
}
